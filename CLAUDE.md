このプロジェクトについて、以下のルールを守って開発をサポートしてください。

■ 技術構成
- React + TypeScript + Vite を使用する
- Next.js は使用しない（App Router / Pages Router どちらも使わない）
- 画面遷移が必要な場合は react-router-dom を使う
- スタイリングは通常のCSSを使用する（Tailwind CSSは使用しない）
- 状態管理は useState / useContext の範囲で行い、Redux等の外部ライブラリは使わない
- データ保存が必要な場合は、まず localStorage を検討する（外部DB・認証は今回は扱わない）

■ 進め方のルール
- 「全部作って」と頼んでも、1つの機能・1つのコンポーネント単位に分けて確認しながら進めてほしい
- 実装するときは、変更したファイル名・変更内容・なぜその実装にしたかを、実装後に簡潔に説明してほしい
- エラーが出たときは、いきなり修正せず、まず「原因は何か」を説明してから直してほしい
- 私はWebアプリ開発初心者です。専門用語を使うときは簡単な説明を添えてほしい

■ 作るアプリについて

【作るアプリ】
- アプリ名：筋トレ記録アプリ（kintore-log）
- 何をするアプリか：筋トレのメニュー（種目・重量・回数・セット）を記録するアプリ。
  既存の記録アプリにない「ドロップセットの記録」「セット間レストの計測」
  「部位ごとのトレーニング間隔の確認」ができるのが特徴。
- 誰が・どんなときに使うか：自分（開発者本人）が、ジムでセットの合間にリアルタイムで入力し、
  トレ前に「今日はどの部位をやるべきか（回復しているか）」を確認するために使う。

【実装する機能】
- 絶対に作りたい機能（必須）
  1. 種目マスタの管理（追加・編集／部位タグ）→ 実装済み（ExercisePicker）
  2. ワークアウトの記録（日付・種目・セット[重量×回数]）＋レストタイマー（セットの記録操作と一体化）
     - 種目を追加した時点で、その種目の直近の記録（履歴から検索）を「前回：60kg×8, 60kg×8, …」のように表示する（表示のみ。今日の入力値には引き継がない＝毎回自分で入力する）
     - 1セットの流れ：①実際にセットをやり終えたら「レスト開始」ボタンを押す →
       レストタイマーが自動スタートすると同時に、その場に重量・回数の入力欄が出る（＋－ボタンで調整、
       2セット目以降は今日の直前セットの値を初期値として引き継ぐ）→ ②レスト中（〜後）に数値を確認・調整して
       「保存」を押すと、そのセットが確定する
     - 各セット行に削除ボタンあり（間違えて記録したとき用）
     - レストタイマーは画面表示のみ（増えていくストップウォッチ）。秒数のデータ保存・計算はしない
  3. ドロップセットの記録（1セット内に重量を下げた段を複数持てる）
  4. 部位別インターバル表示（各部位の最終実施日・経過日数・回復ステータス）
  5. 履歴の閲覧（日付別リスト → 内訳表示）
- 時間があれば追加したい機能
  1. スーパーセット（種目をグループ化して共通レストタイマー）
  2. 種目ごとの重量推移グラフ・自己ベスト
  3. ルーティンのテンプレ／前回のメニューをコピー

【必要な画面】
- ホーム：部位別インターバル＋「トレを開始」ボタン＋直近の記録
- 記録画面（トレ中）：種目を追加 → セット追加（通常／ドロップ）→ 完了でレストタイマー →
  「トレ終了」で履歴に保存。記録はトレ中にリアルタイムで行う（終わってからまとめて入力ではない）。
- 履歴画面：日付別リスト／詳細
- ※「種目マスタ」は独立画面にせず、記録画面から呼び出す ExercisePicker に統合している
  （記録画面と種目マスタが別々に追加・編集機能を持つと二重管理になるため。メンターの指摘を反映）

【コンポーネント】（現状）
- Header（ナビゲーション）
- BodyPartManager（部位の追加・名前変更・削除）／BodyPartIntervalList（部位別インターバル表示）／BodyPartBadge（部位タグ）
- ExercisePicker（種目の検索・一覧・追加・編集・削除。ホームと記録画面の両方から使う）
- ExerciseForm（種目の追加・編集フォーム。ExercisePickerの内部で使用）
- WorkoutEditor（記録画面本体）／ExerciseBlock（種目1つぶん）／SetRow（セット1つぶん、保存済み/レスト中の2状態）
- NumberStepper（＋－ボタンでの数値入力）
- History（履歴画面、一覧の中でその場に開閉する詳細表示）

【データ保存】
- 保存するデータ：部位マスタ／種目マスタ／ワークアウト履歴／進行中のワークアウト
- 保存方法：localStorage（キーは "kintore-log:" プレフィックス付き。src/utils/storage.ts 参照）
- 型定義は src/types.ts にまとめている
- レストタイマーは画面表示のみで、秒数はデータとして保存しない（指示により）

【デプロイ】
- GitHub Pages（`.github/workflows/deploy.yml` でmainにpushするたび自動ビルド・公開）
- 公開URL：https://rintakahata.github.io/kintore-log/
- GitHub側で1回だけ、リポジトリの Settings → Pages → Build and deployment の
  Source を「GitHub Actions」にする設定が必要

■ 実装の状況（進めるたびに更新する）
- [x] プロジェクト作成・ルーティング・スタイルの土台
- [x] 部位マスタ（追加・名前変更・削除）
- [x] 種目マスタ（ExercisePicker / ExerciseForm / BodyPartBadge）
- [x] ワークアウト記録画面（リアルタイム記録＋レスト開始/保存の2段階セット入力）
- [x] ドロップセット
- [x] 部位別インターバル
- [x] 履歴画面
- [x] スマホ向けレスポンシブ調整
- [x] GitHub Pagesへのデプロイ設定（GitHub側の1回だけの設定は別途必要）
- [x] （追加・簡易版）前回と同じ種目で始めるボタン
- [ ] （追加・未着手）スーパーセット：複雑さに対して残り時間が少なく見送った
- [ ] （追加・未着手）重量推移グラフ・自己ベスト：見送った（グラフ描画は次の機能候補）
- [ ] （追加・未着手）名前付きルーティンテンプレ：「前回と同じ種目で始める」で近い効果を先に実装した
